{
  "name": "touchsudoku-records",
  "version": "1.0.0",
  "description": "Touch Sudoku record verification scripts",
  "scripts": {
    "check": "bun run scripts/check-record.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
